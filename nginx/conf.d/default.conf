# Upstream para backend
upstream backend {
    server backend:3001;
}

# Upstream para frontend
upstream frontend {
    server frontend:80;
}

# Redirección HTTP a HTTPS
server {
    listen 80;
    server_name localhost;
    
    # Redireccionar todo a HTTPS
    return 301 https://$server_name$request_uri;
}

# Servidor HTTPS principal
server {
    listen 443 ssl http2;
    server_name localhost;

    # Certificados SSL/TLS (auto-firmados para desarrollo)
    # En producción, usar certificados de Let's Encrypt o CA válida
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;

    # Configuración SSL/TLS segura
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    ssl_session_tickets off;

    # Headers de seguridad
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Ocultar versión de Nginx
    server_tokens off;

    # Logging
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;

    # Configuración de ModSecurity para este servidor (temporalmente desactivado)
    # modsecurity_rules '
    #     SecRuleEngine On
    #     Include /etc/nginx/modsecurity/modsecurity.conf
    # ';

    # Proxy para API REST
    location /api/ {
        # Rate limiting para endpoints de autenticación
        limit_req zone=auth_limit burst=5 nodelay;
        if ($request_uri ~ ^/api/v1/auth/login) {
            limit_req zone=auth_limit burst=3 nodelay;
        }

        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }

    # WebSocket proxy (para el juego y chat)
    location /ws/ {
        # Ajustar reglas ModSecurity para WebSockets (temporalmente desactivado)
        # modsecurity_rules '
        #     SecRuleEngine On
        #     SecRuleRemoveById 942100
        #     SecRuleRemoveById 942110
        #     SecRuleRemoveById 942200
        # ';

        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }

    # Servir frontend estático
    location / {
        proxy_pass http://frontend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Health check endpoint
    location /health {
        access_log off;
        # modsecurity_rules 'SecRuleEngine Off';
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}

