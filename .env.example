# Database (used by docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_DB=transcendence

# Secrets are provided via files in ./secrets (see secrets/README.md)

# OAuth - fill in if you use Google/GitHub login (see learning/TESTING.md section 0)
GOOGLE_ID_CLIENT=
GOOGLE_CLIENT_SECRET=
GITHUB_ID_CLIENT=
GITHUB_CLIENT_SECRET=
