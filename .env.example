# Database (used by docker-compose.yml)
POSTGRES_USER=
POSTGRES_DB=

#NGINX CONFIG
NGINX_PORT_HTTP=
NGINX_PORT_HTTPS=

# OAuth Client IDs 
GOOGLE_CLIENT_ID=
GITHUB_CLIENT_ID=

CLIENT_URL=

# Secrets are provided via files in ./secrets (see secrets/README.md)
